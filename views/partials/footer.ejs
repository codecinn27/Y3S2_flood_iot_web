<footer class="container-fluid fixed-bottom gradient-background">
  <div class="row border-bottom border-4 border-black bg-light">
    <div class="col mx-1 fs-3 fw-bold">
        Status:<span id="status"><%= data.status %></span>
    </div>
    <div id="buttonContainer" class="col-auto mx-1 button_alert" style="display: none;">
      <!-- <button id="toggleButton" class="fs-4" onclick="sendFavoriotCommand(1)"> -->
        <button id="toggleButton" class="fs-4" onclick="sendFavoriotCommandWithModal()">
        <span></span>
          <span></span>
          <span></span>
          <span></span>
          Swiss Gate
      </button>
    </div>
  </div>

    <div class="row text-center fs-3 bg-light">
      <a href="/iot/dashboard/<%= data.id%>" class="col p-2 border-end border-4 border-black">
        <i class="fa-solid fa-table"></i>
      </a>
      <a href="/iot/datalog/<%= data.id%>" class="col p-2 border-end border-4 border-black">
        <i class="fa-solid fa-clock-rotate-left"></i>
      </a>
      <a href="/iot/notification" class="col p-2 border-end border-4 border-black">
        <i class="fa-regular fa-bell"></i>
      </a>
      <a href="/iot/analysis/<%= data.id %>" class="col p-2 border-end border-4 border-black">
        <i class="fa-solid fa-server"></i>
      </a>
      <a href="/iot/about" class="col p-2">
        <i class="fa-regular fa-user"></i>
      </a>
    </div>
</footer>

    
<script>
  function sendFavoriotCommandWithModal() {
      // Call the original function
      sendFavoriotCommand(1);

      // Show the modal confirming that the Swiss Gate has been opened
      var gateModal = new bootstrap.Modal(document.getElementById('gateModal'), { backdrop: true });
      gateModal.show();
  }
</script>

  <!-- Modal -->
  <div class="modal fade" id="gateModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="gateModalLabel" aria-hidden="true">
    <div class="modal-dialog alert alert-success">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="gateModalLabel">Swiss Gate Opened 🚪</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
              <p>🤟🏻🤟🤟🏽Thank you for your cooperation in opening the Swiss Gate. Your actions help prevent floods and protect our community.</p>
            </div>
            <div class="modal-footer d-flex justify-content-between">
                <p>From: FloodEyE 🙆🏽🙆🏽‍♂️</p>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
  </div>


